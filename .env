# Use Compose's normal BuildKit execution path.
# Some Docker Desktop/Buildx versions on Windows fail while Bake parses the
# pinned remote Git context used by the Kong-Role service.
COMPOSE_BAKE=false
